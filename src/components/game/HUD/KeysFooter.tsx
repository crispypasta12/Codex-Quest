"use client";

export function KeysFooter() {
  return (
    <div className="hud-card keys-footer">
      <span><kbd className="kbd">WASD</kbd> / <kbd className="kbd">↑↓←→</kbd> Move</span>
      <span><kbd className="kbd">E</kbd> / <kbd className="kbd">Space</kbd> Interact</span>
      <span><kbd className="kbd">Esc</kbd> Pause</span>
    </div>
  );
}
